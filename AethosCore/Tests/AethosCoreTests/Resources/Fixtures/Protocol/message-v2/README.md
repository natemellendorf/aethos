# Message v2 conformance fixtures

These vectors define canonical message attribution behavior for the breaking v2 contract.

- Canonical author is required and authoritative.
- Transport peer identity is metadata only.
- Unknown structural fields are rejected.
- Unknown extension metadata keys are accepted unless they use reserved namespaces.
